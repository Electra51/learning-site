import React from 'react';
import { Link } from 'react-router-dom';

const CourseDetails = ({ description }) => {
    // const { id } = description;
    
    console.log(description)
    return (
        <div>

        </div>
    );
};

export default CourseDetails;